{
  "name": "texas-power-usage",
  "version": "1.0.0",
  "private": true,
  "description": "Offline Smart Meter Texas CSV explorer",
  "scripts": { "test": "node --test tests/*.test.cjs", "start": "python3 -m http.server 8000" }
}
